//SQL 복습 


데이터베이스는 DB라고하고 
데이터를 저장하는곳이다

그 안에 있는것은 테이블과 
그 테이블 안에는 데이터들이 있다

테이블은 			ㅁㅁㅁㅁㅁ
			ㅁㅁㅁㅁㅁ
			ㅁㅁㅁㅁㅁ
이렇게있다면 가로는 행이라고하고 
		세로를 열이라고 한다.

쉽게 말하자면 데이터베이스는 도서관이고
		테이블은 책장
		데이터는 그안에있는책들이다
		그 책들을 구분하는 토픽으로 구분되어있는 칸들이 열인거고
			그 안에 실제로 있는 책들이 행이다.


우리가 DB를 사용하려면

XAMPP를 실행하고 파일을 만들고 SQLYOG를 사용해 그 파일을 관리할수있다.

쿼리를 사용해서 그 안에서 파일을 관리가 가능한데
가장 중요한 쿼리들(명령어)는

SELECT이다 
SELECT 컬럼명 박으면 그 열 보여줘 
FROM 테이블명; ( 해당테이블로부터 ) 

모든 정보 보고싶으면

SELECT * FROM 테이블명; 박으면됨


여기서 중복 빼고싶으면 Distinct 사용하면됨 
SELECT DISTINCT 과일종류 FROM 창고 ; 
//창고테이블로부터 과일종류 중복빼고 뭐뭐잇는지 보여줘


WHERE 

WHERE은 조건을 나타낸다

SELECT ~~~FROM ~~WHERE 조건이 이런경우

ORDER BY

정렬을 해주는 쿼리

오름차순 ORDER BY 급여 ASC; ( ASCEND 낮 -> 높)
내림차순 ORDER BY 급여 DSC; ( DESCEND 높 ->낮)

INNER JOIN

한 테이블의 정보와 다른 테이블의 정보를 
합칠때 사용

테이블정보 1 2 3      테이블 정보 2 3 4 가있다면
3이라는 교집합점이있기에 
3이라는 교집합점을 바탕으로 좌 테이블정보 23  OR 13 우 테이블정보 23 OR 34 를 같이 
조인해서 
결과값 234 OR 134 OR 123을 만들수있다.

정규화 
제 1정규화는

한 칸에는 하나의 정보값을 넣으라는 얘기임
하나의 정보값을 넣어야 하는이유는
나중에 값수정할떄 더 유용하기때문
좋아하는 음식이라는 열에 해당하는 행에 
탕수육 ,짜장면, 짬봉이라는 세음식이 있다면

나중에 짬봉을 없애주세요 라고 하게된다면 아 저ㄱ ㅓ어떻게해 
하며 야마가 돌게된다
그런거 방지임

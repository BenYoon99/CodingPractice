// 20260406 Java 컴네 개념 독학


 인터프리팅 언어인 파이썬과 자바스크립트 타입스크립트는 속도가 매우느리다

왜 why 일일이 해당 타입스크립트를 전부 해석 후 전달되기에 매우느리다.


자바는 그반에컴파일러 언어라 

중간정도의 속도를 보유하고있다.

컴파일을하게되면

자바의 고유한 언어로 변환 후 
런타임을 거친다.


비동기는 que 영역에서 데이터처리를할때 데이터를 
순차적으로 빼내다가 병목현상이날때 대처하기 좋은 외부 라이브러리이다.
ADHD 마냥 
파일 실행처리를 오더해놓고 
다른 작업들을 하는 것이다.
고로 1을 하고있으면 다른 2 3을 하고있을수있기에 ( 코드가 )
매우 효율적이다
이게 비동기 프로그램인지 확인하려면 코드 입력후 결과값 받을시

결과값으로 <pending> 이나 promise{...} 가 뜨면 비동기 프로그램이다.

동기와의 차이를 세탁기로 이해하자면

동기는 세탁기를 돌려놓고 세탁기가 끝날떄까지 아무것도하지않고 기다리는 반면

비동기는 세탁기를 돌려놓고 거실 청소를 하는것이라고 생각하면 편하다. 그리고 세탁기가 다 돌아가면 알람이 울려 확인하는것과 같다.

- 배열 arr == C의 구조체 str 

배열의 길이는 .length로 계산

배열은 long 이나 double 말고 int로 하는게 좋다 
<-- 왜?

배열은 n-1로 만들어주는거 잊지말고 ㅇㅇ 

new 는 힙영역에 새로운 객체를 생성할때 사용했음 


자료형 [] 배열명= new 자료형 [배열크기];



- 자바 String format 

어떠한 문자열을 미리 정의된 양식으로 맞춰서
'동적'으로 생성할때 사용되는 강력한 도구이다.


구조 

String result = String.format("문자열의 모양 ,값 1...);


예를 들어

String.format( 
"품목 : %-10s 

단가 : %7d원

수량 %7d 개

",
item price qunatity )

이렇게 되어있다면

item은 첫번째 %에
price는 두번째 %에
quantity는 세번째 %에 할당된다.
그럼 %는 항상 ""안에 들어가잇어야해? 
String이니까?
<-- ㅇㅇ 항상 ""안에잇어야하고

그리고 그렇다면 
애초에 여기 

String receipt = String.format(
            "\n[영수증]\n" +
            "품목: %-10s\n" +
            "단가: %,7d원\n" +
            "수량: %7d개\n" +
            "할인: %7.1f%%\n" +
            "------------------\n" +
            "합계: %,7d원",
            item, price, quantity, discount * 100, (int)(price * quantity *discount * 100, (int)(price * quantity * (1 - discount))
        );

        System.out.println(receipt);
    }
}
이것도 매우 더럽게 +로 연결되어있는데
그럼 기본점과 큰차이가 있는게 아닌데
뭐하러 사용하는거임? 


2차원 배열 - 관계 Table /Grid

가로와 세로의 관계로 데이터를 맺고있다.
사용하는 이유 카테고리나 위치정보가 포함될때 사용한다.
국 영 수 시험 관리시

1차원 관리하면 90 90 80 80 80 

2차원 관리하면 

score [1] [2]


스택 ---------------힙

			새로운 객체 생성
			힙영역의 스트링틀 



-자바의 메모리관리 

힙

1. print(listArgument) //메서드 호출

리스트아규먼트라는 참조변수 메소드로 넘겨줌 
메서도 호출시 인자값이 그대로 파라미터에 복사된다.

2. pirnt(list<String > 


<-- 이거 나머지 과정에 대해서도 설명좀 해주라 

3. 메서드 내부 실행

print 안의 파라미터 변수와 main 변수는 힙 영역에 있는 동일 객체를 가리킨다.
만약에 메서드 내부에서 list.add("New Item")을 박으면 주소를 따라가서 힙에 있는 진짜 리스트를 수정하게된다.

4. 메서드 종료시 ( 스택을 청소한다)

메서드 실행이 끝나면 print 메서드의 스택프레임을 통째로 날려버린다.
하지만 힙에 있는 리스트 객체들은 그대로 남아있다
왜냐하면 Main 메서드의 변수가 그 주소를 그대로 붙잡고 존재하고 있기 때문에.


문자열 메서드

equals() : 주소를 신경쓰지않는다, 오직 **안에 
들어있는 문자열의 내용(데이터)이 글자 하나 하나 똑같은가를 확인한다.
내용이 같으면 true 아니면 false

compareTo() : 누가 더 앞서냐( 사전순) 까지 알려준다. 주로 정렬(Sorting)할 떄 사용한다.


== : 주소 비교 연산자

변수가 들고 있는 값 자체를 비교한다.
하지만 String은 참조 변수이기에 
== 사용시 두 변수가 힙 영역의 같은 주소를 가르키고 있는가를 묻는게 된다.
같냐를 물어보는게 같은 주소를 가리키고 있느냐를 물어보는거임 


encapsulation
 캡슐화

필드변수는 프라이빗으로 숨기고

특정 메소드로만 외부 다른 클래스들과 소통하는것.

게터 
해당 필드변수 가져오게하는 메소드
get을 접두사 그리고 이어지는 단어의 첫 글자는 반드시 대문자  ( camelCase)
반드시 리턴값이 있고 매개변수가 없음 
왜 리턴값이 반드시 있냐 ( 필드변수값을 받아와야하기떄문) void 불가

읽기전용

세터
해당 필드변수값 변경/수정등 하게하는 메소드
setName
반드시 리턴값이 없고( 왜냐하면 값을 가져오는게 아닌 해당 필드변수값을 변경하는것이기에)
매개변수가 있다 void 필수


쓰기전용

접근제한자

public protected default private

public은 같은 클래스 멤버 같은 패키지 영역 자식클래스 멤버 기타 영역

전부 0 0 0 0 전부 오케이다 

하지만 protected는 기타영역 x 외 전부 000
디폴트는 00 xx

프라이빗은 0xxx 
같은 클라스의 멤버끼리만 가능하다

상속에서는 다단계 상속이가능하다

부모 클래스를 
상속받는
자식클래스를
상속받는
자식클래스가 
존재 가능하다
하지만 다중상속은 불가능하다
다중상속은
클래스 1과 클래스 2를 한 클래스 3이 상속하는경우이다.
다중상속의 경우에는 interface 사용한다.
일반적 클래스간의 다중상속은 불가능하지만
interface를 사용시 다중상속이 가능해진다.

